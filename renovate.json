{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "customManagers": [
        {
            "customType": "regex",
            "datasourceTemplate": "deb",
            "managerFilePatterns": [
                "/^Dockerfile$/"
            ],
            "matchStrings": [
                "#\\s*renovate:\\s*?(suite=(?<suite>.*?))?\\s*depName=(?<depName>.*?)?\\s(ARG|ENV) .*?_VERSION=\"(?<currentValue>.*)\""
            ],
            "registryUrlTemplate": "https://deb.debian.org/debian?suite={{#if suite }}{{suite}}{{else}}stable{{/if}}&components=main,contrib,non-free&binaryArch=amd64",
            "versioningTemplate": "{{#if versioning}}{{{versioning}}}{{else}}deb{{/if}}"
        },
        {
            "customType": "regex",
            "description": "Update GitHub runner labels in matrix.os",
            "datasourceTemplate": "github-runners",
            "versioningTemplate": "docker",
            "managerFilePatterns": [
                "/^\\.github\\/workflows\\/test\\.ya?ml$/"
            ],
            "matchStrings": [
                "(?m)^\\s*-\\s*(?<depName>ubuntu|windows|macos)-(?<currentValue>latest|\\d{4}|\\d+(?:\\.\\d+)?)\\s*#\\s*renovate:\\s*runner\\s*$"
            ],
            "autoReplaceStringTemplate": "                    - {{depName}}-{{newValue}} # renovate: runner"
        }
    ],
    "extends": [
        "config:best-practices"
    ],
    "lockFileMaintenance": {
        "automerge": true,
        "enabled": true
    },
    "packageRules": [
        {
            "automerge": true,
            "matchDepTypes": [
                "devDependencies"
            ],
            "matchPackageNames": [
                "ruff",
                "ty"
            ]
        },
        {
            "automerge": true,
            "matchCurrentVersion": "!/^0/",
            "matchUpdateTypes": [
                "minor",
                "patch"
            ]
        }
    ],
    "pre-commit": {
        "enabled": true
    }
}
